// $(function(){ // on first doc ready we instantiate mixitup
//     $('#Grid').mixItUp(); // an instance now exists in the session memory
// });

// $(window).on('page:fetch', function(){ // or possibly 'page:before-change'
//     $('#Grid').mixItUp('destroy'); // destroy the instance
// });

// $(window).on('page:load', function(){
//     $('#Grid').mixItUp(); // We can now reinstantiate without being blocked
// });